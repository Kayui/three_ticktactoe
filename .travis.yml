language: java
sudo: false # faster builds
jdk:
  - oraclejdk8
before_script:
  - chmod +x gradlew
  - chmod +x versioncheck.sh
  - ./versioncheck.sh
script:
  - ./gradlew check
after_success:
  - bash <(curl -s https://codecov.io/bash)
